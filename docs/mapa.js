const GAT_SERVERS=[{label:'BIDUZAO - DOUGLAS',url:'https://douglas.tail4577e8.ts.net'},{label:'JC - JEAN',url:'https://jean-jc.tailf14a00.ts.net'}];
const LIVE_PATH='/api/public/live',FRESH_MS=16000,REF_ZOOM=8;
const TILE_URL='https://raw.githubusercontent.com/felix-d1strict/vtc-map/master/ets2map/coloured/{z}/{x}_{y}.png';
let mapDrivers=[],currentFilter='all',firstPosition=true;
const truckMarkers=new Map();
const norm=s=>String(s||'').trim().toLowerCase();
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const age=t=>{const d=Date.parse(t||'');return Number.isFinite(d)?Date.now()-d:Infinity};
const fresh=t=>age(t?.updated_at)<=FRESH_MS;
const hasPosition=t=>{if(!fresh(t))return false;const x=Number(t?.map_x),z=Number(t?.map_z);return Number.isFinite(x)&&Number.isFinite(z)&&(Math.abs(x)>.01||Math.abs(z)>.01)};
const truckText=t=>{const a=String(t?.truck_make||'').trim(),b=String(t?.truck_model||'').trim();return [a,b].filter(Boolean).join(' ')||'Caminhão não informado'};

const liveMap=L.map('ets2Map',{crs:L.CRS.Simple,minZoom:0,maxZoom:8,zoomControl:true,attributionControl:false,preferCanvas:true});
const mapBounds=L.latLngBounds(liveMap.unproject([0,192512],REF_ZOOM),liveMap.unproject([173568,0],REF_ZOOM));
L.tileLayer(TILE_URL,{minZoom:0,maxZoom:8,tileSize:512,bounds:mapBounds,noWrap:true,keepBuffer:3}).addTo(liveMap);
liveMap.setMaxBounds(mapBounds.pad(.08));
liveMap.setView(liveMap.unproject([90000,90000],REF_ZOOM),2);

function gameToLatLng(x,z){return liveMap.unproject([Number(x)/.78125+89600,Number(z)/.78125+89600],REF_ZOOM)}
function headingDeg(v){const h=Number(v);if(!Number.isFinite(h))return 0;if(Math.abs(h)<=1.01)return h*360;if(Math.abs(h)<=Math.PI*2+.01)return h*180/Math.PI;return h%360}
function markerKey(d){return d.server+'|'+norm(d.name)}
function markerIcon(d){const moving=fresh(d.t)&&d.t.on_job,deg=headingDeg(d.t?.map_heading);return L.divIcon({className:'gat-truck-icon',iconSize:[48,48],iconAnchor:[24,24],html:'<div class="gat-truck-marker '+(moving?'':'idle')+'"><div class="gat-truck-arrow" style="transform:rotate('+deg.toFixed(1)+'deg)"><div class="gat-truck-body"></div></div><span class="gat-truck-label">'+esc(d.name)+'</span></div>'})}
function popupHtml(d){const t=d.t||{},route=t.on_job?(String(t.source||'Origem')+' → '+String(t.destination||'Destino')):'Online • sem carga',speed=Math.round(Number(t.speed_kmh)||0),acct=String(t.account_user||'').trim();return '<div class="gat-popup"><b>'+esc(d.name)+'</b><small>'+esc(d.server)+'</small><strong>'+esc(truckText(t))+'</strong><small>'+esc(route)+'</small><small>'+speed+' km/h'+(acct?' • @'+esc(acct):'')+'</small></div>'}

async function fetchServer(s){const c=new AbortController(),timer=setTimeout(()=>c.abort(),4500);try{const r=await fetch(s.url+LIVE_PATH,{cache:'no-store',signal:c.signal});if(!r.ok)throw 0;const data=await r.json();return {s,data};}catch(_){return {s,data:null};}finally{clearTimeout(timer)}}
function build(results){const out=[];results.forEach(({s,data})=>{if(!data)return;const players=Array.isArray(data.players)?data.players:[],tel=Array.isArray(data.telemetry)?data.telemetry:[],by=new Map(tel.map(t=>[norm(t.driver),t]));players.forEach(p=>out.push({server:s.label,name:p,t:by.get(norm(p))||null}));tel.filter(t=>fresh(t)&&!players.some(p=>norm(p)===norm(t.driver))).forEach(t=>out.push({server:s.label,name:t.driver,t}));});const seen=new Set();return out.filter(x=>{const k=markerKey(x);if(seen.has(k))return false;seen.add(k);return true})}
function renderList(){const root=document.getElementById('mapDrivers');root.textContent='';const list=mapDrivers.filter(d=>currentFilter==='all'||(currentFilter==='trip'&&fresh(d.t)&&d.t.on_job)||(currentFilter==='idle'&&(!d.t||!d.t.on_job)));document.getElementById('mapDriverCount').textContent=list.length;if(!list.length){root.innerHTML='<div class="map-loading">Nenhum motorista nesse filtro.</div>';return;}list.forEach(d=>{const a=document.createElement('a');a.className='map-driver-item';const acct=String(d.t?.account_user||'').trim();a.href='motorista.html?u='+encodeURIComponent((acct||d.name).replace(/^@/,'').toLowerCase());const route=fresh(d.t)&&d.t.on_job?(d.t.source||'Origem')+' → '+(d.t.destination||'Destino'):(fresh(d.t)?'Online • sem carga':'Sem telemetria recente'),speed=fresh(d.t)?Math.round(Number(d.t.speed_kmh)||0):0,pos=hasPosition(d.t);a.innerHTML='<div class="map-driver-avatar">'+esc(String(d.name||'?').charAt(0).toUpperCase())+'</div><div><b>'+esc(d.name||'Motorista')+'</b><small>'+esc(route)+'</small><small class="map-driver-truck">'+esc(truckText(d.t))+'</small></div><div class="map-driver-speed"><strong>'+speed+'</strong><br>km/h<i class="'+(pos?'positioned':'')+'"></i></div>';root.appendChild(a)})}
function renderPins(){const positioned=mapDrivers.filter(d=>hasPosition(d.t));const active=new Set(),latlngs=[];positioned.forEach(d=>{const key=markerKey(d),ll=gameToLatLng(d.t.map_x,d.t.map_z);active.add(key);latlngs.push(ll);let marker=truckMarkers.get(key);if(!marker){marker=L.marker(ll,{icon:markerIcon(d),zIndexOffset:d.t?.on_job?500:300}).addTo(liveMap);truckMarkers.set(key,marker)}else{marker.setLatLng(ll);marker.setIcon(markerIcon(d));marker.setZIndexOffset(d.t?.on_job?500:300)}marker.bindPopup(popupHtml(d),{closeButton:false,offset:[0,-12]})});for(const [key,marker] of truckMarkers){if(!active.has(key)){liveMap.removeLayer(marker);truckMarkers.delete(key)}}document.getElementById('coordinateNotice').style.display=positioned.length?'none':'block';if(firstPosition&&latlngs.length){firstPosition=false;if(latlngs.length===1)liveMap.setView(latlngs[0],6);else liveMap.fitBounds(L.latLngBounds(latlngs),{padding:[55,55],maxZoom:6})}}
async function refresh(){const results=await Promise.all(GAT_SERVERS.map(fetchServer));mapDrivers=build(results);renderList();renderPins();const online=mapDrivers.length,trips=mapDrivers.filter(d=>fresh(d.t)&&d.t.on_job).length,positioned=mapDrivers.filter(d=>hasPosition(d.t)).length;document.getElementById('mapOnline').textContent=online;document.getElementById('mapTrips').textContent=trips;document.getElementById('mapTelemetry').textContent=positioned;document.getElementById('mapClock').textContent=new Date().toLocaleTimeString('pt-BR');const ok=results.some(r=>r.data),badge=document.getElementById('mapLiveState');badge.textContent=ok?'● AO VIVO':'● SEM CONEXÃO';badge.classList.toggle('online',ok)}
document.querySelectorAll('.map-filters button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.map-filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');currentFilter=b.dataset.filter;renderList()}));
window.addEventListener('resize',()=>liveMap.invalidateSize());
setTimeout(()=>liveMap.invalidateSize(),250);
refresh();setInterval(refresh,3000);